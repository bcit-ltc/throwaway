apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "throwaway.fullname" . }}-test-connection"
  labels:
    {{- include "throwaway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "throwaway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
